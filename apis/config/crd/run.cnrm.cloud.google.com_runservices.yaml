---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: stable
    cnrm.cloud.google.com/system: "true"
  name: runservices.run.cnrm.cloud.google.com
spec:
  group: run.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: RunService
    listKind: RunServiceList
    plural: runservices
    shortNames:
    - gcprunservice
    - gcprunservices
    singular: runservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: RunService is the Schema for the RunService API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RunServiceSpec defines the desired state of RunService
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Optional. User-provided annotations, which are stored
                  in GCP.
                type: object
              binaryAuthorization:
                description: Optional. Settings for Binary Authorization feature.
                properties:
                  breakglassJustification:
                    description: |-
                      Optional. If present, indicates to use Breakglass using this justification.
                       If use_default is False, then it must be empty.
                       For more information on breakglass, see
                       https://cloud.google.com/binary-authorization/docs/using-breakglass
                    type: string
                  useDefault:
                    description: |-
                      Optional. If True, indicates to use the default project's binary
                       authorization policy. If False, binary authorization will be disabled.
                    type: boolean
                type: object
              buildConfig:
                description: Optional. Configuration for building a Cloud Run function.
              client:
                description: Optional. Arbitrary identifier for the API client.
                type: string
              clientVersion:
                description: Optional. Arbitrary version identifier for the API client.
                type: string
              customAudiences:
                description: Optional. One or more custom audiences that you want
                  this service to support.
                items:
                  type: string
                type: array
              defaultURIDisabled:
                description: Optional. Disables public resolution of the default URI
                  of this service.
                type: boolean
              description:
                description: Optional. User-provided description of the Service.
                type: string
              ingress:
                description: Optional. Provides the ingress settings for this Service.
                type: string
              invokerIAMDisabled:
                description: Optional. Disables IAM permission check for run.routes.invoke
                  for callers of this service.
                type: boolean
              launchStage:
                description: Optional. The launch stage of the service.
                type: string
              location:
                description: The location of the cloud run service
                type: string
              projectRef:
                description: The project that this resource belongs to.
                properties:
                  external:
                    description: The `projectID` field of a project, when not managed
                      by Config Connector.
                    type: string
                  kind:
                    description: The kind of the Project resource; optional but must
                      be `Project` if provided.
                    type: string
                  name:
                    description: The `name` field of a `Project` resource.
                    type: string
                  namespace:
                    description: The `namespace` field of a `Project` resource.
                    type: string
                type: object
              resourceID:
                description: The RunService name. If not given, the metadata.name
                  will be used.
                type: string
              scaling:
                description: Optional. Specifies service-level scaling settings
              template:
                description: Required. The template used to create revisions for this
                  Service.
              traffic:
                description: Optional. Specifies how to distribute traffic over a
                  collection of Revisions belonging to the Service.
                items: {}
                type: array
            required:
            - template
            type: object
          status:
            description: RunServiceStatus defines the config connector machine state
              of RunService
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the
                  object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: |-
                        Unique, one-word, CamelCase reason for the condition's last
                        transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              createTime:
                description: Output only. The creation time.
                type: string
              creator:
                description: Output only. Email address of the authenticated creator.
                type: string
              deleteTime:
                description: Output only. The deletion time.
                type: string
              etag:
                description: Output only. A system-generated fingerprint for this
                  version of the resource.
                type: string
              expireTime:
                description: Output only. For a deleted resource, the time after which
                  it will be permanently deleted.
                type: string
              externalRef:
                description: A unique specifier for the RunService resource in GCP.
                type: string
              lastModifiedCookie:
                description: LastModifiedCookie contains hashes of the last applied
                  spec and the last observed GCP state.
                type: string
              lastModifier:
                description: Output only. Email address of the last authenticated
                  modifier.
                type: string
              latestCreatedRevision:
                description: Output only. Name of the last created revision.
                type: string
              latestReadyRevision:
                description: Output only. Name of the latest revision that is serving
                  traffic.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                format: int64
                type: integer
              reconciling:
                description: Output only. Returns true if the Service is currently
                  being acted upon by the system to bring it into the desired state.
                type: boolean
              terminalCondition:
                description: Output only. The Condition of this Service, containing
                  its readiness status.
                items:
                  properties:
                    executionReason:
                      description: A reason for the execution condition.
                      type: string
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human readable message indicating details about
                        the current status.
                      type: string
                    reason:
                      description: A common (service-level) reason for this condition.
                      type: string
                    revisionReason:
                      description: A reason for the revision condition.
                      type: string
                    severity:
                      description: How to interpret failures of this condition, one
                        of Error, Warning, Info
                      type: string
                    state:
                      description: State of the condition.
                      type: string
                    type:
                      description: |-
                        type is used to communicate the status of the reconciliation process.
                         See also:
                         https://github.com/knative/serving/blob/main/docs/spec/errors.md#error-conditions-and-reporting
                         Types common to all resources include:
                         * "Ready": True when the Resource is ready.
                      type: string
                  type: object
                type: array
              trafficStatuses:
                description: Output only. Detailed status information for corresponding
                  traffic targets.
                items: {}
                type: array
              uid:
                description: Output only. Server assigned unique identifier for the
                  trigger.
                type: string
              updateTime:
                description: Output only. The last-modified time.
                type: string
              uri:
                description: Output only. The main URI in which this Service is serving
                  traffic.
                type: string
              urls:
                description: Output only. All URLs serving traffic for this Service.
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
