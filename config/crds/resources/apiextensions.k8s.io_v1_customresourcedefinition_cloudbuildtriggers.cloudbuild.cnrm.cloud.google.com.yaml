# Generated by dev/tasks/generate-crds.  DO NOT EDIT.

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cnrm.cloud.google.com/version: 0.0.0-dev
  creationTimestamp: null
  labels:
    cnrm.cloud.google.com/managed-by-kcc: "true"
    cnrm.cloud.google.com/stability-level: stable
    cnrm.cloud.google.com/system: "true"
    cnrm.cloud.google.com/tf2crd: "true"
  name: cloudbuildtriggers.cloudbuild.cnrm.cloud.google.com
spec:
  group: cloudbuild.cnrm.cloud.google.com
  names:
    categories:
    - gcp
    kind: CloudBuildTrigger
    listKind: CloudBuildTriggerList
    plural: cloudbuildtriggers
    shortNames:
    - gcpcloudbuildtrigger
    - gcpcloudbuildtriggers
    singular: cloudbuildtrigger
  preserveUnknownFields: false
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: When 'True', the most recent reconcile of the resource succeeded
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The reason for the value in 'Ready'
      jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Status
      type: string
    - description: The last transition time for the value in 'Status'
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: Status Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: CloudBuildTrigger is the Schema for the CloudBuildTrigger API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CloudBuildTriggerSpec defines the desired state of CloudBuildTrigger
            properties:
              approvalConfig:
                description: Configuration for manual approval to start a build invocation
                  of this BuildTrigger.
                properties:
                  approvalRequired:
                    description: Whether or not approval is needed. If this is set
                      on a build, it will become pending when created, and will need
                      to be explicitly approved to start.
                    type: boolean
                type: object
              bitbucketServerTriggerConfig:
                description: BitbucketServerTriggerConfig describes the configuration
                  of a trigger that creates a build whenever a Bitbucket Server event
                  is received.
                properties:
                  bitbucketServerConfigResourceRef:
                    description: 'The full resource name of the bitbucket server config.
                      Format: projects/{project}/locations/{location}/bitbucketServerConfigs/{id}.'
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed CloudBuildBitbucketServerConfig
                          resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/bitbucketServerConfigs/{{bitbucketServerConfigID}}".
                        type: string
                      name:
                        description: The name of a CloudBuildBitbucketServerConfig
                          resource.
                        type: string
                      namespace:
                        description: The namespace of a CloudBuildBitbucketServerConfig
                          resource.
                        type: string
                    type: object
                  projectKey:
                    description: 'Key of the project that the repo is in. For example:
                      The key for https://mybitbucket.server/projects/TEST/repos/test-repo
                      is "TEST".'
                    type: string
                  pullRequest:
                    description: Filter to match changes in pull requests.
                    properties:
                      branch:
                        description: |-
                          Regex of branches to match.

                           The syntax of the regular expressions accepted is the syntax accepted by
                           RE2 and described at https://github.com/google/re2/wiki/Syntax
                        type: string
                      commentControl:
                        description: |-
                          If CommentControl is enabled, depending on the setting, builds may not
                           fire until a repository writer comments `/gcbrun` on a pull
                           request or `/gcbrun` is in the pull request description.
                           Only PR comments that contain `/gcbrun` will trigger builds.

                           If CommentControl is set to disabled, comments with `/gcbrun` from a user
                           with repository write permission or above will
                           still trigger builds to run.
                        type: string
                      invertRegex:
                        description: If true, branches that do NOT match the git_ref
                          will trigger a build.
                        type: boolean
                    type: object
                  push:
                    description: Filter to match changes in refs like branches, tags.
                    properties:
                      branch:
                        description: |-
                          Regexes matching branches to build.

                           The syntax of the regular expressions accepted is the syntax accepted by
                           RE2 and described at https://github.com/google/re2/wiki/Syntax
                        type: string
                      invertRegex:
                        description: When true, only trigger a build if the revision
                          regex does NOT match the git_ref regex.
                        type: boolean
                      tag:
                        description: |-
                          Regexes matching tags to build.

                           The syntax of the regular expressions accepted is the syntax accepted by
                           RE2 and described at https://github.com/google/re2/wiki/Syntax
                        type: string
                    type: object
                  repoSlug:
                    description: Slug of the repository. A repository slug is a URL-friendly
                      version of a repository name, automatically generated by Bitbucket
                      for use in the URL. For example, if the repository name is 'test
                      repo', in the URL it would become 'test-repo' as in https://mybitbucket.server/projects/TEST/repos/test-repo.
                    type: string
                required:
                - bitbucketServerConfigResourceRef
                - projectKey
                - repoSlug
                type: object
              build:
                description: Contents of the build template.
                properties:
                  artifacts:
                    properties:
                      images:
                        items:
                          type: string
                        type: array
                      objects:
                        properties:
                          location:
                            type: string
                          paths:
                            items:
                              type: string
                            type: array
                          timing:
                            description: 'Timing is Output only in proto, but in Spec
                              in existing CRD? CRD: timing: description: Output only.
                              ... If it is output only, why is it in Spec? Maybe Terraform
                              allows setting it? Unlikely. I will add it to match
                              CRD.'
                            items:
                              properties:
                                endTime:
                                  type: string
                                startTime:
                                  type: string
                              type: object
                            type: array
                        type: object
                    type: object
                  availableSecrets:
                    properties:
                      secretManager:
                        items:
                          properties:
                            env:
                              type: string
                            versionRef:
                              oneOf:
                              - not:
                                  required:
                                  - external
                                required:
                                - name
                              - not:
                                  anyOf:
                                  - required:
                                    - name
                                  - required:
                                    - namespace
                                required:
                                - external
                              properties:
                                external:
                                  description: ' If provided must be in the format
                                    `projects/*/secrets/*/versions/*`.'
                                  type: string
                                name:
                                  description: The `name` field of a `SecretManagerSecretVersion`
                                    resource.
                                  type: string
                                namespace:
                                  description: The `metadata.namespace` field of a
                                    `SecretManagerSecretVersion` resource.
                                  type: string
                              type: object
                          required:
                          - env
                          - versionRef
                          type: object
                        type: array
                    type: object
                  images:
                    items:
                      type: string
                    type: array
                  logsBucketRef:
                    description: StorageBucketRef defines the resource reference to
                      StorageBucket, which "External" field holds the GCP identifier
                      for the KRM object.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally-managed StorageBucket
                          resource.
                        type: string
                      name:
                        description: The name of a StorageBucket resource.
                        type: string
                      namespace:
                        description: The namespace of a StorageBucket resource.
                        type: string
                    type: object
                  options:
                    properties:
                      diskSizeGb:
                        format: int64
                        type: integer
                      dynamicSubstitutions:
                        type: boolean
                      env:
                        items:
                          type: string
                        type: array
                      logStreamingOption:
                        type: string
                      logging:
                        type: string
                      machineType:
                        type: string
                      requestedVerifyOption:
                        type: string
                      secretEnv:
                        items:
                          type: string
                        type: array
                      sourceProvenanceHash:
                        items:
                          type: string
                        type: array
                      substitutionOption:
                        type: string
                      volumes:
                        items:
                          properties:
                            name:
                              type: string
                            path:
                              type: string
                          type: object
                        type: array
                      workerPool:
                        type: string
                    type: object
                  queueTtl:
                    type: string
                  secret:
                    items:
                      properties:
                        kmsKeyRef:
                          oneOf:
                          - not:
                              required:
                              - external
                            required:
                            - name
                          - not:
                              anyOf:
                              - required:
                                - name
                              - required:
                                - namespace
                            required:
                            - external
                          properties:
                            external:
                              description: A reference to an externally managed KMSCryptoKey.
                                Should be in the format `projects/[kms_project_id]/locations/[region]/keyRings/[key_ring_id]/cryptoKeys/[key]`.
                              type: string
                            name:
                              description: The `name` of a `KMSCryptoKey` resource.
                              type: string
                            namespace:
                              description: The `namespace` of a `KMSCryptoKey` resource.
                              type: string
                          type: object
                        secretEnv:
                          additionalProperties:
                            type: string
                          type: object
                      required:
                      - kmsKeyRef
                      type: object
                    type: array
                  source:
                    properties:
                      repoSource:
                        properties:
                          branchName:
                            type: string
                          commitSha:
                            type: string
                          dir:
                            type: string
                          invertRegex:
                            type: boolean
                          projectId:
                            type: string
                          repoRef:
                            oneOf:
                            - not:
                                required:
                                - external
                              required:
                              - name
                            - not:
                                anyOf:
                                - required:
                                  - name
                                - required:
                                  - namespace
                              required:
                              - external
                            properties:
                              external:
                                description: The external name of the SourceRepoRepository
                                  resource.
                                type: string
                              name:
                                description: The name of the SourceRepoRepository
                                  resource.
                                type: string
                              namespace:
                                description: The namespace of the SourceRepoRepository
                                  resource.
                                type: string
                            type: object
                          substitutions:
                            additionalProperties:
                              type: string
                            type: object
                          tagName:
                            type: string
                        required:
                        - repoRef
                        type: object
                      storageSource:
                        properties:
                          bucketRef:
                            description: StorageBucketRef defines the resource reference
                              to StorageBucket, which "External" field holds the GCP
                              identifier for the KRM object.
                            oneOf:
                            - not:
                                required:
                                - external
                              required:
                              - name
                            - not:
                                anyOf:
                                - required:
                                  - name
                                - required:
                                  - namespace
                              required:
                              - external
                            properties:
                              external:
                                description: A reference to an externally-managed
                                  StorageBucket resource.
                                type: string
                              name:
                                description: The name of a StorageBucket resource.
                                type: string
                              namespace:
                                description: The namespace of a StorageBucket resource.
                                type: string
                            type: object
                          generation:
                            type: string
                          object:
                            type: string
                        required:
                        - bucketRef
                        - object
                        type: object
                    type: object
                  step:
                    items:
                      properties:
                        allowExitCodes:
                          items:
                            format: int32
                            type: integer
                          type: array
                        allowFailure:
                          type: boolean
                        args:
                          items:
                            type: string
                          type: array
                        dir:
                          type: string
                        entrypoint:
                          type: string
                        env:
                          items:
                            type: string
                          type: array
                        id:
                          type: string
                        name:
                          type: string
                        script:
                          type: string
                        secretEnv:
                          items:
                            type: string
                          type: array
                        timeout:
                          type: string
                        timing:
                          type: string
                        volumes:
                          items:
                            properties:
                              name:
                                type: string
                              path:
                                type: string
                            type: object
                          type: array
                        waitFor:
                          items:
                            type: string
                          type: array
                      required:
                      - name
                      type: object
                    type: array
                  substitutions:
                    additionalProperties:
                      type: string
                    type: object
                  tags:
                    items:
                      type: string
                    type: array
                  timeout:
                    type: string
                required:
                - step
                type: object
              description:
                description: Human-readable description of the trigger.
                type: string
              disabled:
                description: Whether the trigger is disabled or not. If true, the
                  trigger will never result in a build.
                type: boolean
              filename:
                description: Path, from the source root, to a file whose contents
                  is used for the template.
                type: string
              filter:
                description: A Common Expression Language string.
                type: string
              gitFileSource:
                description: The file source describing the local or remote Build
                  template.
                properties:
                  bitbucketServerConfigRef:
                    description: Bitbucket Server Config resource name.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed CloudBuildBitbucketServerConfig
                          resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/bitbucketServerConfigs/{{bitbucketServerConfigID}}".
                        type: string
                      name:
                        description: The name of a CloudBuildBitbucketServerConfig
                          resource.
                        type: string
                      namespace:
                        description: The namespace of a CloudBuildBitbucketServerConfig
                          resource.
                        type: string
                    type: object
                  githubEnterpriseConfigRef:
                    description: 'The full resource name of the github enterprise
                      config. Format: `projects/{project}/locations/{location}/githubEnterpriseConfigs/{id}`.
                      `projects/{project}/githubEnterpriseConfigs/{id}`.'
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: The external name of the CloudBuildGithubEnterpriseConfig
                          resource.
                        type: string
                      name:
                        description: The name of the CloudBuildGithubEnterpriseConfig
                          resource.
                        type: string
                      namespace:
                        description: The namespace of the CloudBuildGithubEnterpriseConfig
                          resource.
                        type: string
                    type: object
                  path:
                    description: The path of the file, with the repo root as the root
                      of the path.
                    type: string
                  repoType:
                    description: See RepoType above.
                    type: string
                  repositoryRef:
                    description: The fully qualified resource name of the Repos API
                      repository. Either URI or repository can be specified. If unspecified,
                      the repo from which the trigger invocation originated is assumed
                      to be the repo from which to read the specified path.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: The external name of the CloudBuildV2Repository
                          resource.
                        type: string
                      name:
                        description: The name of the CloudBuildV2Repository resource.
                        type: string
                      namespace:
                        description: The namespace of the CloudBuildV2Repository resource.
                        type: string
                    type: object
                  revision:
                    description: 'The branch, tag, arbitrary ref, or SHA version of
                      the repo to use when resolving the filename (optional). This
                      field respects the same syntax/resolution as described here:
                      https://git-scm.com/docs/gitrevisions If unspecified, the revision
                      from which the trigger invocation originated is assumed to be
                      the revision from which to read the specified path.'
                    type: string
                  uri:
                    description: The URI of the repo. Either uri or repository can
                      be specified. If unspecified, the repo from which the trigger
                      invocation originated is assumed to be the repo from which to
                      read the specified path.
                    type: string
                required:
                - path
                - repoType
                type: object
              github:
                description: |-
                  GitHubEventsConfig describes the configuration of a trigger that creates
                   a build whenever a GitHub event is received.

                   Mutually exclusive with `trigger_template`.
                properties:
                  installationID:
                    description: The installationID that emits the GitHub event.
                    format: int64
                    type: integer
                  name:
                    description: 'Name of the repository. For example: The name for
                      https://github.com/googlecloudplatform/cloud-builders is "cloud-builders".'
                    type: string
                  owner:
                    description: 'Owner of the repository. For example: The owner
                      for https://github.com/googlecloudplatform/cloud-builders is
                      "googlecloudplatform".'
                    type: string
                  pullRequest:
                    description: filter to match changes in pull requests.
                    properties:
                      branch:
                        description: |-
                          Regex of branches to match.

                           The syntax of the regular expressions accepted is the syntax accepted by
                           RE2 and described at https://github.com/google/re2/wiki/Syntax
                        type: string
                      commentControl:
                        description: |-
                          If CommentControl is enabled, depending on the setting, builds may not
                           fire until a repository writer comments `/gcbrun` on a pull
                           request or `/gcbrun` is in the pull request description.
                           Only PR comments that contain `/gcbrun` will trigger builds.

                           If CommentControl is set to disabled, comments with `/gcbrun` from a user
                           with repository write permission or above will
                           still trigger builds to run.
                        type: string
                      invertRegex:
                        description: If true, branches that do NOT match the git_ref
                          will trigger a build.
                        type: boolean
                    type: object
                  push:
                    description: filter to match changes in refs like branches, tags.
                    properties:
                      branch:
                        description: |-
                          Regexes matching branches to build.

                           The syntax of the regular expressions accepted is the syntax accepted by
                           RE2 and described at https://github.com/google/re2/wiki/Syntax
                        type: string
                      invertRegex:
                        description: When true, only trigger a build if the revision
                          regex does NOT match the git_ref regex.
                        type: boolean
                      tag:
                        description: |-
                          Regexes matching tags to build.

                           The syntax of the regular expressions accepted is the syntax accepted by
                           RE2 and described at https://github.com/google/re2/wiki/Syntax
                        type: string
                    type: object
                type: object
              ignoredFiles:
                description: |-
                  ignored_files and included_files are file glob matches using
                   https://golang.org/pkg/path/filepath/#Match extended with support for "**".

                   If ignored_files and changed files are both empty, then they are not
                   used to determine whether or not to trigger a build.

                   If ignored_files is not empty, then we ignore any files that match any
                   of the ignored_file globs. If the change has no files that are outside
                   of the ignored_files globs, then we do not trigger a build.
                items:
                  type: string
                type: array
              includeBuildLogs:
                description: Build logs will be sent back to GitHub as part of the
                  checkrun result.  Values can be INCLUDE_BUILD_LOGS_UNSPECIFIED or
                  INCLUDE_BUILD_LOGS_WITH_STATUS
                type: string
              includedFiles:
                description: |-
                  If any of the files altered in the commit pass the ignored_files filter
                   and included_files is empty, then as far as this filter is concerned, we
                   should trigger the build.

                   If any of the files altered in the commit pass the ignored_files filter
                   and included_files is not empty, then we make sure that at least one of
                   those files matches a included_files glob. If not, then we do not trigger
                   a build.
                items:
                  type: string
                type: array
              location:
                description: The location of this resource.
                type: string
              pubsubConfig:
                description: PubsubConfig describes the configuration of a trigger
                  that creates a build whenever a Pub/Sub message is published.
                properties:
                  serviceAccountRef:
                    description: Service account that will make the push request.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: The `email` field of an `IAMServiceAccount` resource.
                        type: string
                      name:
                        description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                        type: string
                      namespace:
                        description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                        type: string
                    type: object
                  state:
                    description: Potential issues with the underlying Pub/Sub subscription
                      configuration. Only populated on get requests.
                    type: string
                  subscription:
                    description: Output only. Name of the subscription.
                    type: string
                  topicRef:
                    description: The name of the topic from which this subscription
                      is receiving messages.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed PubSubTopic
                          resource. Should be in the format "projects/{{projectID}}/topics/{{topicID}}".
                        type: string
                      name:
                        description: The name of a PubSubTopic resource.
                        type: string
                      namespace:
                        description: The namespace of a PubSubTopic resource.
                        type: string
                    type: object
                required:
                - topicRef
                type: object
              repositoryEventConfig:
                description: The configuration of a trigger that creates a build whenever
                  an event from Repo API is received.
                properties:
                  pullRequest:
                    description: Filter to match changes in pull requests.
                    properties:
                      branch:
                        description: |-
                          Regex of branches to match.

                           The syntax of the regular expressions accepted is the syntax accepted by
                           RE2 and described at https://github.com/google/re2/wiki/Syntax
                        type: string
                      commentControl:
                        description: |-
                          If CommentControl is enabled, depending on the setting, builds may not
                           fire until a repository writer comments `/gcbrun` on a pull
                           request or `/gcbrun` is in the pull request description.
                           Only PR comments that contain `/gcbrun` will trigger builds.

                           If CommentControl is set to disabled, comments with `/gcbrun` from a user
                           with repository write permission or above will
                           still trigger builds to run.
                        type: string
                      invertRegex:
                        description: If true, branches that do NOT match the git_ref
                          will trigger a build.
                        type: boolean
                    type: object
                  push:
                    description: Filter to match changes in refs like branches, tags.
                    properties:
                      branch:
                        description: |-
                          Regexes matching branches to build.

                           The syntax of the regular expressions accepted is the syntax accepted by
                           RE2 and described at https://github.com/google/re2/wiki/Syntax
                        type: string
                      invertRegex:
                        description: When true, only trigger a build if the revision
                          regex does NOT match the git_ref regex.
                        type: boolean
                      tag:
                        description: |-
                          Regexes matching tags to build.

                           The syntax of the regular expressions accepted is the syntax accepted by
                           RE2 and described at https://github.com/google/re2/wiki/Syntax
                        type: string
                    type: object
                  repository:
                    description: The resource name of the Repo API resource.
                    type: string
                type: object
              serviceAccountRef:
                description: IAM service account whose credentials will be used at
                  build runtime.
                oneOf:
                - not:
                    required:
                    - external
                  required:
                  - name
                - not:
                    anyOf:
                    - required:
                      - name
                    - required:
                      - namespace
                  required:
                  - external
                properties:
                  external:
                    description: The `email` field of an `IAMServiceAccount` resource.
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                    type: string
                  namespace:
                    description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                    type: string
                type: object
              sourceToBuild:
                description: The repo and ref of the repository from which to build.
                  This field is used only for those triggers that do not respond to
                  SCM events. Triggers that respond to such events build source at
                  whatever commit caused the event. This field is currently only used
                  by Webhook, Pub/Sub, Manual, and Cron triggers.
                properties:
                  bitbucketServerConfigRef:
                    description: Bitbucket Server Config resource name.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: A reference to an externally managed CloudBuildBitbucketServerConfig
                          resource. Should be in the format "projects/{{projectID}}/locations/{{location}}/bitbucketServerConfigs/{{bitbucketServerConfigID}}".
                        type: string
                      name:
                        description: The name of a CloudBuildBitbucketServerConfig
                          resource.
                        type: string
                      namespace:
                        description: The namespace of a CloudBuildBitbucketServerConfig
                          resource.
                        type: string
                    type: object
                  githubEnterpriseConfigRef:
                    description: 'The full resource name of the github enterprise
                      config. Format: `projects/{project}/locations/{location}/githubEnterpriseConfigs/{id}`.
                      `projects/{project}/githubEnterpriseConfigs/{id}`.'
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: The external name of the CloudBuildGithubEnterpriseConfig
                          resource.
                        type: string
                      name:
                        description: The name of the CloudBuildGithubEnterpriseConfig
                          resource.
                        type: string
                      namespace:
                        description: The namespace of the CloudBuildGithubEnterpriseConfig
                          resource.
                        type: string
                    type: object
                  ref:
                    description: The branch or tag to use. Must start with "refs/"
                      (required).
                    type: string
                  repoType:
                    description: See RepoType below.
                    type: string
                  repositoryRef:
                    description: The connected repository resource name, in the format
                      `projects/*/locations/*/connections/*/repositories/*`. Either
                      `uri` or `repository` can be specified and is required.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: The external name of the CloudBuildV2Repository
                          resource.
                        type: string
                      name:
                        description: The name of the CloudBuildV2Repository resource.
                        type: string
                      namespace:
                        description: The namespace of the CloudBuildV2Repository resource.
                        type: string
                    type: object
                  uri:
                    description: The URI of the repo (e.g. https://github.com/user/repo.git).
                      Either `uri` or `repository` can be specified and is required.
                    type: string
                required:
                - ref
                - repoType
                type: object
              substitutions:
                additionalProperties:
                  type: string
                description: Substitutions for Build resource.
                type: object
              tags:
                description: Tags for annotation of a `BuildTrigger`
                items:
                  type: string
                type: array
              triggerTemplate:
                description: |-
                  Template describing the types of source changes to trigger a build.

                   Branch and tag names in trigger templates are interpreted as regular
                   expressions. Any branch or tag change that matches that regular
                   expression will trigger a build.

                   Mutually exclusive with `github`.
                properties:
                  branchName:
                    description: |-
                      Regex matching branches to build.

                       The syntax of the regular expressions accepted is the syntax accepted by
                       RE2 and described at https://github.com/google/re2/wiki/Syntax
                    type: string
                  commitSha:
                    description: Explicit commit SHA to build.
                    type: string
                  dir:
                    description: |-
                      Directory, relative to the source root, in which to run the build.

                       This must be a relative path. If a step's `dir` is specified and is an
                       absolute path, this value is ignored for that step's execution.
                    type: string
                  invertRegex:
                    description: Only trigger a build if the revision regex does NOT
                      match the revision regex.
                    type: boolean
                  repoRef:
                    description: The Cloud Source Repository to build. If omitted,
                      the repo with name "default" is assumed.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: The external name of the SourceRepoRepository
                          resource.
                        type: string
                      name:
                        description: The name of the SourceRepoRepository resource.
                        type: string
                      namespace:
                        description: The namespace of the SourceRepoRepository resource.
                        type: string
                    type: object
                  tagName:
                    description: |-
                      Regex matching tags to build.

                       The syntax of the regular expressions accepted is the syntax accepted by
                       RE2 and described at https://github.com/google/re2/wiki/Syntax
                    type: string
                type: object
              webhookConfig:
                description: WebhookConfig describes the configuration of a trigger
                  that creates a build whenever a webhook is sent to a trigger's webhook
                  URL.
                properties:
                  secretRef:
                    description: Required. Resource name for the secret required as
                      a URL parameter.
                    oneOf:
                    - not:
                        required:
                        - external
                      required:
                      - name
                    - not:
                        anyOf:
                        - required:
                          - name
                        - required:
                          - namespace
                      required:
                      - external
                    properties:
                      external:
                        description: ' If provided must be in the format `projects/*/secrets/*/versions/*`.'
                        type: string
                      name:
                        description: The `name` field of a `SecretManagerSecretVersion`
                          resource.
                        type: string
                      namespace:
                        description: The `metadata.namespace` field of a `SecretManagerSecretVersion`
                          resource.
                        type: string
                    type: object
                  state:
                    description: Potential issues with the underlying Pub/Sub subscription
                      configuration. Only populated on get requests.
                    type: string
                required:
                - secretRef
                type: object
            type: object
          status:
            description: CloudBuildTriggerStatus defines the config connector machine
              state of CloudBuildTrigger
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the object's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be True,
                        False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  type: object
                type: array
              createTime:
                description: Time when the trigger was created.
                type: string
              externalRef:
                description: A unique specifier for the CloudBuildTrigger resource
                  in GCP.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation of the resource
                  that was most recently observed by the Config Connector controller.
                  If this is equal to metadata.generation, then that means that the
                  current reported status reflects the most recent desired state of
                  the resource.
                format: int64
                type: integer
              observedState:
                description: ObservedState is the state of the resource as most recently
                  observed in GCP.
                type: object
              triggerId:
                description: The unique identifier for the trigger.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
