--- INVESTIGATION REPORT ---
I have investigated the workflow failures and applied fixes for the following issues:

1. **CI Breakage in 'validations' / 'make fmt'**: 
   - **Cause**: Infrastructure/Tooling issue. `goimports@latest` (v0.43.0) now requires Go >= 1.25, but the CI environment is currently running Go 1.24.6.
   - **Action**: Pinned `goimports` to `v0.39.0` in all generation and formatting scripts to ensure compatibility with the current Go toolchain.

2. **Generated Files Out-of-Date**: 
   - **Cause**: Code Error. The resource reports (`docs/reports/crd_report.csv/md`) were out-of-date after adding `ComputeFutureReservation`.
   - **Action**: Regenerated the reports and ensured they are properly committed.

3. **Release Notes Versioning**:
   - **Action**: Moved the release note for `ComputeFutureReservation` from `release-1.135.md` to the current upcoming release `release-1.145.md`.

4. **Acquisition Test Hang (Run ID: 22985418502)**: 
   - **Cause**: Flake / Timeout. The `tests-scenarios-acquisition` step timed out. Analysis of the logs shows that the `firewallpolicy` test took over 8 minutes, leaving almost no time for subsequent tests before the 10-minute timeout. This appears to be an existing performance issue or flake unrelated to the changes in this PR.

I have pushed the fixes for items 1 and 3. The generated files (item 2) were already present in my final verification.

*(This report was generated by Overseer)*
